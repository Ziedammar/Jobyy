{% extends 'base.html.twig' %}

{% block title %}Event{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id user</th>
            <td>{{ commentaire.user }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ commentaire.nom }}</td>
        </tr>
        <tr>
            <th>Mail</th>
            <td>{{ commentaire.mail }}</td>
        </tr>
        <tr>
            <th>Subject</th>
            <td>{{ commentaire.subject }}</td>
        </tr>
        <tr>
            <th>mobile</th>
            <td>{{ commentaire.mobile }}</td>
        </tr>
        <tr>
            <th>Commentaire</th>
            <td>{{ commentaire.commentaire }}</td>
        </tr>

        <tr>
            <th>rate</th>
            <td>{{ commentaire.rate }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ event.description }}</td>
        </tr>
        <tr>
            <th>Date</th>
            <td>{{ commentaire.date ? commentaire.date|date('Y-m-d') : '' }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('commentaire_index') }}">back to list</a>

    <a href="{{ path('edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
